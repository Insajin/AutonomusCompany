# ⚠️  DEPLOYMENT EXAMPLE: GitHub Pages (Static Site)
#
# 📌 HOW TO USE THIS FILE:
# 1. Enable GitHub Pages in repository settings
# 2. Set source to "GitHub Actions"
# 3. Copy to: .github/workflows/deploy-github-pages.yml
# 4. Push to trigger deployment
#
# 📋 REQUIRED CONFIGURATION:
#   - Repository Settings > Pages > Source: "GitHub Actions"
#   - No secrets needed! 🎉
#
# 📚 Documentation: https://pages.github.com/
# 💰 Pricing: Free for public repositories
# ⚡ Benefits:
#   - Completely free
#   - Easy setup
#   - Automatic HTTPS
#   - Perfect for documentation sites
# ⚠️  Considerations:
#   - Static sites only (no server-side code)
#   - Public repositories only (free tier)
#   - 1GB size limit
#   - 100GB/month soft bandwidth limit

name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - 'frontend/**'
      - '!frontend/**/*.md'

permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment
concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Install dependencies
        working-directory: frontend
        run: npm ci

      - name: Build application
        working-directory: frontend
        run: npm run build
        env:
          CI: true
          # Next.js specific: enable static export
          # Add "output: 'export'" to next.config.js

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./frontend/out  # or ./frontend/build for React

  deploy:
    name: Deploy
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

      - name: Report deployment
        run: |
          echo "✅ Deployed to GitHub Pages"
          echo "🌍 Site URL: ${{ steps.deployment.outputs.page_url }}"
