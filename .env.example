# ============================================
# Environment Variables Template
# ============================================
#
# SETUP INSTRUCTIONS:
# 1. Copy this file to .env: cp .env.example .env
# 2. Fill in the actual values (remove "your_*_here" placeholders)
# 3. NEVER commit .env to version control (it's in .gitignore)
# 4. Add required secrets to GitHub repository secrets for CI/CD
#
# See SETUP.md for detailed instructions on getting these values

# ============================================
# Claude Code OAuth Token (REQUIRED FOR AI FEATURES)
# ============================================
# This token enables Claude Code to:
# - Review pull requests automatically
# - Apply AI-suggested fixes
# - Generate feature suggestions
# - Create implementation PRs
#
# How to get this token:
# 1. Install Claude Code CLI from: https://claude.com/code
# 2. Run: claude
# 3. Execute: /install-github-app
# 4. Follow the authentication prompts
# 5. Copy the generated token and paste below
# 6. Add this same token to GitHub Secrets: CLAUDE_CODE_OAUTH_TOKEN
#
# Cost: No additional API costs - uses your Claude Max subscription
# Security: Token is repository-scoped for security

CLAUDE_CODE_OAUTH_TOKEN=your_oauth_token_here


# ============================================
# Deployment Configuration (OPTIONAL)
# ============================================
# Only configure these if you're using the deployment workflow (.github/workflows/deploy.yml)
# If not deploying, you can:
# - Leave these empty
# - Delete .github/workflows/deploy.yml
# - The workflow will automatically skip if secrets are not configured

# --- Vercel (Frontend Deployment) ---
# For deploying frontend (Next.js, React, etc.) to Vercel
#
# How to get these values:
# 1. VERCEL_TOKEN: Go to https://vercel.com/account/tokens → Create new token
# 2. VERCEL_ORG_ID & VERCEL_PROJECT_ID: Create project in Vercel → Check Settings → General
#
# Then add these to GitHub Secrets for CI/CD deployment:
VERCEL_TOKEN=your_vercel_token_here
VERCEL_ORG_ID=your_vercel_org_id
VERCEL_PROJECT_ID=your_vercel_project_id

# --- Docker Hub (Backend Deployment) ---
# For deploying backend as Docker container
#
# How to get these values:
# 1. DOCKER_USERNAME: Your Docker Hub username
# 2. DOCKER_PASSWORD: Create access token at https://hub.docker.com/settings/security
#    (Do NOT use your Docker Hub password - use an access token for security)
# 3. DOCKER_REGISTRY:
#    - docker.io (Docker Hub - default)
#    - ghcr.io (GitHub Container Registry)
#    - your-registry.com (private registry)
DOCKER_USERNAME=your_docker_username
DOCKER_PASSWORD=your_docker_access_token_here
DOCKER_REGISTRY=docker.io


# ============================================
# Application Configuration (OPTIONAL)
# ============================================
# Application-specific environment variables

# Database
DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# API Keys
# Add any API keys your application needs
# API_KEY=your_api_key_here

# Application Settings
NODE_ENV=development
PORT=3000


# ============================================
# GitHub Configuration (OPTIONAL)
# ============================================
# Only needed for advanced project board automation

# GitHub Personal Access Token
# Scope required: repo, project, write:discussion
# Generate at: https://github.com/settings/tokens
GITHUB_TOKEN=your_github_pat_here

# GitHub Projects
# Get project ID from: gh project list
GITHUB_PROJECT_ID=your_project_id_here


# ============================================
# Monitoring & Analytics (OPTIONAL)
# ============================================

# Sentry (Error Tracking)
SENTRY_DSN=your_sentry_dsn_here

# DataDog (Monitoring)
DATADOG_API_KEY=your_datadog_api_key

# Google Analytics
GA_TRACKING_ID=your_ga_tracking_id


# ============================================
# Notes
# ============================================
# - Required secrets must be added to GitHub repository secrets
# - Go to: Settings > Secrets and variables > Actions
# - Add each secret individually
# - Secrets are encrypted and not exposed in logs
# - Update this file when adding new secrets to workflows
